# タブ10：環境見出し・構成の位置づけ・相手要約の検証

2026-09-16。単独実装。実装commit: `3215aa4`、比較元: `d6abf7c`。push・デプロイなし。

## 原因と変更

|項目|確認した原因|修正と結果|
|---|---|---|
|F1|2Dの環境名が各囲いの左上へ直接置かれ、同じ境界位置で衝突した|環境名を高さ予約済みの見出し領域へ分離し、囲い側の安定した番号・補助線と対応。ノード配置を変えず、ズーム後も番号の画面上サイズを維持|
|F2|位置づけの継承元がnpm packageだけで、配下の独立manifestへ直下packageの説明を割り当てた|manifest/.NET定義を所有者に含め、明示的所属を優先。定義位置・用途・判定根拠を分離。workspace設定による所属はmatchesとEvidenceで確認。補助表示は選択可能なまま控えめにし、同名の短いパスは識別部分を優先|
|F3|共通の先頭3相手枠を未特定要求集合が占有した|構成上の相手と未特定要求に別の3件枠・追加表示ボタンを設けた。要求の元IDで重複排除し、対象・表示集合・ソース箇所を別集計|

HTTP判定は変更せず、既存テストで回帰確認した。関係線の経路、全体配置、粒子量、自動省略の方式は変更していない。

## 入力と一般性

ブラウザは前回作業で保存されたローカル固定入力を使用。git-lines 1,814ファイル、vehicle-management 194ファイル、web-atlas 244ファイル。これらは従前のブラウザ用スコープ（100KB未満・test/spec/fixtures除外）であり、製品の入力除外規則を追加したものではない。vehicle-managementには実際の配下Package.swiftとSources/main.swiftの2ファイルを読み取り専用で追加して196ファイルで検証した。原本を変更・実行・外部送信していない。

通常の全テストでも3プロジェクトの既存固定入力検証が成功。独立定義は異なる名前・深さの2サンプル、長い/近接する環境名、同名別ID、既知/未特定のみ、双方向関係、集合と個別の重複を追加テストした。

実際のSwift定義は、定義位置に配下のPackage.swift、用途に「実行構成として定義」を表示。「プロジェクトルート」や主要用途として説明しない。git-linesはmanifestの名称・説明による従来の補助推定を保持し、`ff-continuity/after`、`capture-all`等で識別できた。深さや特定フォルダー名による補助分類は追加していない。

## 実ブラウザ結果

Chrome、ローカルVite、1440×1000を基準に検証。

- 修正前のgit-lines：developmentと環境未特定の見出しが両方とも x=589.928、y=232.834。Fit時の文字高は約2.3pxだった。
- 修正後：環境名は12px、見出しの行高約20px。囲い側の番号は別々に配置され、見出し領域をクリップで保護。実装途中で番号がナビゲーション下へ隠れることを発見し、下方への衝突回避に再修正した。
- 1440/1024/768/390px、通常・全画面、パン・ズーム後を確認。見出しの重なりとページ横はみ出しなし。390pxの簡略ナビゲーションでも見出しは残る。
- vehicle-managementのweb：構成上の相手7、未特定対象86、表示集合3、ソース88箇所。先頭にAPI・共有コード・入力コードが表示され、追加ボタンで7相手と未特定4項目すべてへ到達できた。
- 2DのEnter選択、3Dの単クリックでscope/ノード座標/保存カメラが一致。内部を開く→戻るでは、2回の描画フレーム完了後に元scope・座標・カメラへ復元した。
- 3Dは既存の衝突回避・密度に応じたラベル表示を維持。補助の識別部分、選択/関連強調、囲いON/OFF/ONを確認。全体Fitでは環境ラベルが省略される条件があり、常に全環境名が出る仕様へ変更していない。
- Runtime Flow / Data Flow / Data Modelを往復し、新しい環境見出しが他タブへ出ないこと、Architecture Mapに戻ると復元することを確認。
- 最終再読込以後の確認区間で新たなconsole errorなし。実装途中のHMRでimport追加前の一時エラーがあったが、修正後の再読込で再現しない。

ローカル画像（入力由来の名称を含むためGit管理外）:

- `.cache/heading-review/before-git-lines.png` / `after-git-lines.png`
- `.cache/heading-review/git-lines-zoom.png` / `git-lines-narrow-fullscreen.png` / `git-lines-3d.png`
- `.cache/heading-review/nested-definition.png` / `peer-summary.png` / `vehicle-3d.png`
- `.cache/heading-review/web-atlas.png`

## 件数・根拠と性能

位置づけの変更は説明属性に限定。元ID・architectureメタデータ・Evidenceの不変性を単体テストで照合。実入力の既存検証も、元関係IDの被覆、集約の重複、所属、要求元、方向を確認して通過した。F3の分類は種別/request/集約メンバーを参照し、関係のconfidenceを変更しない。個別表示された要求と集合内の同じsourceIdは一度だけ数える。

同じweb-atlas入力・1440px・全画面Fitで、先頭3ノードをEnter選択して2描画フレームまで測定。修正前119/53/48ms、修正後111/57/43ms。Zoom inは前後とも67ms。Playwright操作時間を含む少数回の簡易測定であり、性能向上や厳密なINPを示すものではない。目視でも停止・目立つ悪化なし。見出し番号の配置はregion/position/scaleの変更時にmemo化し、panや毎フレームで全データを再走査しない。

## テストと制約

- `pnpm build`、`pnpm typecheck`、`pnpm lint`、`git diff --check`成功。ビルドはサンドボックスの親ディレクトリ読取制限で初回失敗後、許可されたローカル再実行で成功。既存のtree-sitter/bundleサイズ警告あり。
- 全回帰：125ファイル、1,337テスト成功、8ファイル/13テストは条件付きskip（86.54秒）。その後追加したUI表示上限テストを含め、相手要約/文脈テスト15件成功。合計1,338件の成功を確認したが、最後のUIテスト追加後の全スイート再実行はしていない。
- 実Swift定義のopt-in検証も1件成功（21.42秒）。
- 未検証：3プロジェクトの無制限全ファイル入力でのブラウザ再検証、全OS/ブラウザ組合せ、極端に多数の環境名。長い名前の一般性は単体テストと折り返しCSSで検証し、実入力に存在しない長名の専用ブラウザ画面は作成していない。
- 単/ダブルクリック分離・外側選択・個別要求取り出しは既存回帰テストを維持。今回の実ブラウザでは全組合せを再演していない。
- 今回確認したF1〜F3に未解決の再現症状なし。従前の無制限大入力のメモリ制約は本作業で再調査していない。
