# タブ10：環境名の直接表示・分類と詳細説明の検証

2026-09-17。単独実装。着手時は `小規模修正３` / `cbc88f5`、作業ツリーはクリーン。実装コミット `2caeede`。

## 原因・変更・確認

|項目|原因|変更・確認|
|---|---|---|
|F1|2Dが離れた番号表との照合を要求していた。3Dは囲いとDOM所属索引のID判定が不一致で、環境ラベルを表示対象から落としていた|2Dは13px・最大2行の環境名を可視境界の近くへ直接配置。ノード・見出し・操作領域を避け、補助線で対応。3Dは共通のID判定と投影した境界上の候補を使用。回転追従と囲いOFF/ONを確認|
|F2|compositionRoleは定義元から継承される場合があるが、選択対象自身の断定のように表示していた|自身の種類はarchitecture.kind、継承説明はdefinitionOwnerIdの構成名付きで「定義元の構成の位置づけ」と表示。解析上の種類・親・用途判定は変更していない|
|F3|概要で完全パス、workspace注意書き、位置づけの根拠が重複し、相手要約より先に面積を消費していた|概要はファイル名・識別に必要な短いパス・短い位置区分。完全パス/コピー・workspace根拠・用途判定理由を既存の「分類・同一性の判定理由」へ集約。役割・環境・操作対象・実行場所・未観測状態を維持|

動画は139秒のうち5/45/90/130秒のフレームを読み、拡大された囲い、Vite、長いcsproj定義、3Dの状態を確認した。撮影範囲の見切れを修正理由にはしていない。

## 実入力・独立サンプル

- Chrome / localhost、通常・全画面。前回から保存されているブラウザ用固定入力を使用：git-lines 1,814ファイル、web-atlas 244ファイル、vehicle-management 194ファイル＋実Swift定義/ソース2ファイル＝196。
- ブラウザ入力は従前の100KB未満・test/spec/fixtures等を除く検証スコープ。今回、製品の入力除外規則は追加していない。実原本の変更・実行・外部送信なし。
- 既存の3プロジェクト固定入力テストも全回帰内で成功。一般性は任意の環境名、日本語、長い共有環境名、同名別ID、異なる定義位置、成果物/操作への継承説明をテストした。
- 独立ブラウザサンプルは本番の見出し配置関数・表示部品に4つの境界を与え、1024/390pxで確認。同名2件は別IDで保持。長文はEnterで全文を開き、13pxを維持した。

## 操作と代表結果

- 2D：vehicle-managementでdevelopment/production/環境未特定/既定設定/共有/論理定義を直接表示。git-linesの隣接見出しを個別の位置に配置。50px/30pxのパン後、対応する境界と名前が追従した。ズームでも文字サイズを保つ。
- 1440/1024/768/390pxでページ横はみ出しなし。詳細パネルのscrollWidthとclientWidthが一致（例349/289/363px）。狭幅で同じカメラを保持すると境界の可視部分と空き領域が変わり、表示される見出し数も変わる。画面外の名称を中央へ集めない。
- 3D：git-linesで回転前2見出し→回転後3見出し。IDを保持して座標が変わり、旧位置にラベルが残らない。囲いOFFで名称0件、ONで3件に復元。既存の重要点・選択ラベル優先と密度上限を維持しているため、すべての囲い名が常時同時表示されるわけではない。
- 2D Enter選択・3D単クリックでscope/ノード位置/保存カメラが一致。見出しの調整で自動Fitしない。
- Migration SQL：自身は「成果物」、役割は入力/出力として指定された成果物。定義の文脈はvehicle-management-apiと説明。関連だけの構成を新しい親にはしていない。
- Drizzle Kit：自身は「操作・ツール使用」、定義元の構成はvehicle-management-api。対象環境の未特定、操作対象Migration SQL、実行場所未確認、設定読込済み・生成未観測を保持した。
- LegacyHost：概要の定義元を `VehicleManagement.LegacyHost.csproj` に短縮。用途と環境を保持し、完全な定義位置は明示展開で読める。
- Migration SQLの判定理由を明示展開し、「定義パスをコピー」で得たクリップボード文字列と画面の完全なパスが一致することをChromeで確認。
- web：構成上の相手7、未特定対象86、表示集合3、ソース88箇所で前回と一致。相手と未特定要求の分離・各追加表示ボタンを保持した。
- Runtime Flow / Function Call Flow / Data Flow / Data Modelへの往復で、新しい2D見出しが出ないことを確認。Architecture Mapへ戻ると再表示。

代表画面はローカル保存（入力由来の名前を含むためGit管理外）：

- [2Dの環境名](../../.cache/direct-headings/vehicle-2d-final.png)
- [3Dの環境名](../../.cache/direct-headings/vehicle-3d-final.png)
- [git-linesの2D](../../.cache/direct-headings/git-lines-2d.png)
- [git-linesの回転後3D](../../.cache/direct-headings/git-lines-3d-rotated.png)
- [成果物の詳細](../../.cache/direct-headings/migration-detail.png)
- [長い定義元の概要](../../.cache/direct-headings/legacy-detail.png)
- [長い環境名の全文・390px](../../.cache/direct-headings/long-narrow.png)

## データ・性能・回帰

解析上の元ID・関係・Evidence・件数の生成処理は変更していない。変更したflowRegionsは既存の判定を共通関数に抽出し、DOMラベルの所属索引を一致させたもの。web-atlasの同じ入力で修正前後の表示ノードID・関係ID・全ノード名/親・ノード座標が一致。単体テストでも、説明生成が元の種類/属性を変更しないことを確認した。

同じweb-atlas入力、1440px、全画面Fit、先頭3ノードのEnter選択→2描画フレームをPlaywrightで測定。修正前65/57/78ms、修正後61/54/74ms。Zoom inは71→74ms。操作ツールの時間を含む少数回測定であり、速度向上の証明ではない。パン/回転は目視と位置追従を確認し、専用のフレーム時間測定は行っていない。

2D配置計算はカメラ/可視領域/ノード位置などの変更時にmemo化。粒子だけの毎フレームで全データを再計算しない。3Dは既存labelDirty時の投影・衝突回避を再利用し、環境境界の候補だけを追加した。

- 着手時の関連テスト47件成功。
- 全回帰：125ファイル、1,343テスト成功。8ファイル/13テストは既存条件付きskip。90.61秒。
- 実画面での最終文字幅/補助線/余白調整後、関連52件成功、typecheck・lint成功。全スイートはその軽微な表示調整後には再実行していない。
- 最終 `pnpm build` 成功（14.90秒）。既存tree-sitter/browser外部化・eval・bundleサイズ警告あり。
- `git diff --check` / staged check成功。
- 検証中、比較用ファイルを展開した際にWindowsのViteファイル監視がEBUSYで停止。展開完了後に再起動して復旧。製品の実行時エラーとは区別した。最終再読込以後の確認区間で新しいconsole errorなし。モード/HMR破棄時のContext Lostログは発生した。

HTTP要求判定は既存テストによる通常回帰だけで維持した。検出・解決の再調査は行っていない。

## 残る確認範囲

今回確認したF1〜F3の症状に未解決の再現はない。未検証は、無制限の全ファイル入力でのブラウザ再検証、全OS/ブラウザ/支援技術の組合せ、極端に多数の境界が同じ狭い領域を占める入力、3Dの厳密なフレーム時間。単/ダブルクリック分離、個別要求の取り出し、全階層の往復などは既存回帰テストを維持し、実ブラウザで全組合せは再演していない。

UI Evolveではrepresentative-verificationとstate-and-copyを参照。今回は「重ならない」だけでなく、名前・説明対象を画面から読み取れるかを動画/実画面で照合した。これは既存知見の適用範囲であり、機器固有の挙動を新しい一般ルールとして追加していない。3DのID索引の不一致はリポジトリ固有の知見として保存する。

push・本番デプロイなし。
