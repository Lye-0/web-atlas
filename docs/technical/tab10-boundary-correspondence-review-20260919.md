# タブ10「簡易全体」：囲い・対応情報・操作検証

2026-09-19。基準HEAD c475a68、ブランチ「小規模修正３」、開始時clean。サブエージェントなし。プロジェクト移動後のWeb_Services/web-atlasで実施。

## F1：原因と局所補正

元所属は正しい。囲いは共有コード配置後の座標で再計算されており、古いキャッシュが原因ではなかった。共有コードが利用元の140座標単位上に置かれ、下側の棚の見出しがそのカードを避けて上へ押し上がった。2D描画で見出しのborderTopまで枠を延ばすため、起動領域と重なった。

簡易専用配置の最後で、棚の見出し・カード外形・最終枠に必要な空間を確保する。関連する共有コードを所属メンバーとは扱わず、先に見出し余白から外す。なお衝突する場合だけ近傍共有コードを含む棚を局所的にずらし、後続の棚へ伝播させる。毎フレームや選択時には実行しない。

最終のvehicle-managementは独立構成5点がy=1840→1892（+52）、補助3点が2060→2084（+24）。共有コード4点の座標、全ての主経路、共通公開経路は不変。Companion側の共有コードと利用元の縦距離は140→192で、近くに保持。git-lines、web-atlasは全点の座標不変。所属・関係は変更なし。

[修正前](../../.cache/boundary-review/boundary-before.png) / [修正後](../../.cache/boundary-review/boundary-current.png)。同じ保存入力、1600×1100、2D全画面、Emulatorを選択→選択へ移動→縮小2回。初案の単純な棚全体移動を採用せず、共有コードを囲いに取り込まない再修正後の画像。

## F2：概要と対応の内訳

概要・役割・環境の後に原本対応を置き、相手の元端点IDでまとめる。同じ相手へのコード使用・論理定義・配信対象という意味は併記する。表示名や環境名の一致ではまとめない。関係を確認済みとして一括認定しない。

APIは3対応先・6元関係。通常表示は相手3件、展開すると6つの元関係を別々に確認できる。最大6対応先の短い概要と、既存の検索・追加表示による完全な内訳を併用。9同名対象・18元関係のUI fixtureで、別ID、根拠パス、表示上限、元対象へのボタンを検証した。

[通常表示](../../.cache/boundary-review/api-after.png) / [展開後](../../.cache/boundary-review/api-expanded.png) / [760px幅](../../.cache/boundary-review/api-narrow.png)。完全な方向、条件、環境、確度、Evidenceは元関係の内訳に保持。

## F3：既存機能の正常確認

表示コード変更なし。2Dの描画済み個別線をキーボードEnterで選択し、右側詳細を確認、その選択のまま3Dへ切り替えて同じ元対象が表示されることを照合した。3Dの線を直接ポインターで拾う全ケースは未網羅。

- development DB適用 → development / local の1対象。
- production DB適用 → productionの1対象。
- 既定設定・production・development各実行構成からの設定/アクセス → 各々に対応する1対象。
- 論理APIからの設定/利用要約線 → 既定設定・development・productionの3対象。
- 計10本の線で確認。集合全体の4対象一覧とは区別。
- 独立fixtureではpreviewと環境未特定の2対象を含む要約線、別の未解決接続を2D/3Dで選択確認。未解決側へ既知DBを割り当てず、releaseの別対象も混入しない。

[複数対象の3D詳細](../../.cache/boundary-review/d1-multiple-3d.png)。対応内容はローカル成果物edge-results.json、部分未特定のfixtureはpartial.model.jsonとブラウザ操作ログに保持。

## F4：参照・使用条件への実操作

表示機能の追加修正なし。

- Vite host指定の使用から、開始scriptの注記、個別関係、package.json L10のCommand fragment pnpm dev:webを展開した。定義パスをコピーし入力欄への貼り付けで一致を確認。全体の元関係へ移動できた。
- 完全コマンド `vite --mode development --host 127.0.0.1` を展開・コピーし、実際の貼り付け結果と一致。元のVite使用へ明示ボタンで移動できた。
- Wrangler起動のdev:api・devの2呼び出しを別々に開き、package.jsonの根拠と完全コマンドを確認。
- Firebase CLI起動のdevからの呼び出しを開き、package.jsonの根拠と完全コマンドを確認。
- summaryのキーボード操作、参照関係の検索・解除を実施。名称の短縮、引数の同等性、既存の秘密情報保護処理は変更なし。

[完全コマンド](../../.cache/boundary-review/vite-command.png) / [参照根拠](../../.cache/boundary-review/vite-reference-evidence.png)。クリップボードAPIの読み取り許可はなかったため、コピーの検証はUIへの貼り付けで行った。実タッチ端末は未検証。

## 元データ・表示・操作

3保存モデルについて元モデルJSON、表示ID、元ID→要約対応、元関係一覧、注記一覧、Evidenceを含む関係データが一致。全体と全既存プリセットの投影を基準版と深い比較で照合し、一致した。表示点/線/注記束数はvehicle 39/59/14、git-lines 9/10/2、web-atlas 9/12/6で不変。

通常選択、対応欄開閉、ホバーでは配置・カメラ・scope不変。パン・ズームでノード位置不変。全体/簡易全体と2D/3Dの往復で保存状態を復元。git-lines、web-atlasの原本から内部へ入り、戻るとprojectと元座標へ戻る。通常検索と解除、全画面の詳細開閉、760px幅の詳細展開を確認。狭幅のdocument幅は760pxで水平のはみ出しなし。

[git-lines 2D](../../.cache/boundary-review/git-lines-2d.png) / [3D](../../.cache/boundary-review/git-lines-3d.png)、[web-atlas 2D](../../.cache/boundary-review/web-atlas-2d.png) / [3D](../../.cache/boundary-review/web-atlas-3d.png)。ブラウザのアプリエラー記録はなし。既存ブラウザセッションのサイズ変更でCLIがEOFを返したため、専用セッションの空ページで760pxへ設定して狭幅を検証した。補助ツールの失敗をアプリ回帰と扱っていない。

## テストと性能

- 修正前：140ファイル、1,436成功、23skip。
- 修正後：141ファイル、1,441成功、24skip。今回追加の任意実入力ゲートが通常skipとなるため1件増えている。
- SHELF_REVIEW=1の3入力・全プリセット比較を含む専用テスト：5成功。
- lint / typecheck / build / git diff --check：成功。tree-sitterのfs/path外部化と大きなchunkのビルド警告は既存。
- 正負例：別名・日本語名、近傍共有、隣接棚、再適用不変、通常親子への非適用、同名別ID、関係別の確度・方向・Evidence、多数対応、部分未特定、未解決。HTTP判定と他タブは通常回帰テストで確認。
- 単発の投影比較：vehicle 273.0→261.8ms、git-lines 376.9→352.3ms、web 159.7→163.4ms。測定ばらつきを含み、速度改善の主張はしない。補正は初回の簡易配置だけ、キャッシュ同一性を確認。

## 範囲と未確認

ブラウザ比較は同じ保存解析モデル、通常回帰は保存された3プロジェクトのソースを読み取り解析した。3プロジェクトの現在の作業フォルダ全件を今回新規に再解析した結果ではない。入力側script・任意の設定コード・起動・公開・DB適用は実行していない。原本変更、外部送信、push、デプロイなし。

長時間FPS/メモリ計測、実タッチ、全カメラ角度、3Dの全線への直接ポインター選択、全プロジェクトの全操作組合せは未網羅。線の回り込み、粒子量、自動省略、HTTP判定の再設計は対象外。確認範囲で未解決の製品不具合はない。

今回のUI知見は既存のrepresentative-verificationを適用。新しい一般ルールではなく、このリポジトリの最終borderTopに関する制約をrepository memoryへ独立保存した。
