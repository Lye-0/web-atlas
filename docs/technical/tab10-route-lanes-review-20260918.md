# 簡易全体：原本ごとの経路・分岐・合流の仕上げ

2026-09-18。開始HEAD `33e40cb`、ブランチ `小規模修正３`、開始時clean。単独実装。実入力のコマンドは実行せず、原本変更・外部送信・push・デプロイなし。

## F1〜F5の原因・対応

| 項目 | 確認した原因 | 変更と確認 |
|---|---|---|
| F1 経路の隣接 | 単一原本の対象を列ごとに並べ、共有部分を後置していた。すべての到着先を最終列へ移したため短い枝も長くなった | 入力からの有限な原本集合で経路帯を構成し、共通部分は関連原本の帯の間へ置く。帯内は到着先から操作へ位置を対応させ、単一枝の操作と到着先を同じ行にする。到着列は実際の段階で決める |
| F2 縦一列 | 経路のない原本・共有コード・補助にも全幅の行があった | 経路なしの独立構成を複数列の領域へ配置。共有部分・利用先は関係元近くの側列、補助/未確認は識別付きの小領域へ配置。消去・強制統合はしない |
| F3 関係の役割 | 元の参照種類ごとに平行線が残り、列配置だけでは縦方向の参照が強かった | 入力/生成/起動/公開/適用だけで主段階を作る。コード参照・依存・呼出等は相手と向き・確度・条件を保持した束へまとめ、個別種類・環境は元関係の内訳に残す。HTTP・DB等の通常接続や条件付き先行関係はそのまま |
| F4 同名操作 | Vite devは追加引数なし、rootからのdev:webはmode/host指定あり。同名・同環境だけでは同等でない | 別使用を保持し、環境・引数・異なる設定/入力/出力等を名称と補足へ表示。図と詳細の相手一覧で同じ名称を使用。同等aliasは要約し、入力関係の条件・確度と設定確認状態も照合 |
| F5 役割 | Auth EmulatorとSuiteはいずれもexternal-serviceでflow-startsの到着先。ただし親を持つ外部対象を内部扱いで飛ばし、未割当contextへ落とす投影経路があった | 外部サービス・資源・外部プログラムは親設定があっても元の独立対象として保持。起動関係を持つ対象は到着側へ配置。元の種類・ID・所属設定は変更しない |

配置上の原本集合は所有者を新設しない。共有公開操作とその到着先は一つのまま、両原本の入力線を保持する。共通成果物後の分岐も保持。SCCの同深度内は再帰せず、訪問済み対象を再配置しない。全経路列挙は行わない。配置順は実行済み・実行順の証明ではない。

枝の見出しは用途/対象環境を文字で表示する。最初の実画面で、狭い行間により既存見出し回避が前の枝へ押し上げる問題を確認し、枝の行間へ文字高さ分の余白を追加した。共通の見出し回避や線回り込みは変更していない。

## 3入力と代表画面

同じ保存解析結果に新旧の投影を適用。原本ファイルの新規全走査ではない。通常テストでは各プロジェクトの固定された現行ソースも読み取り検証する。

| 入力 | 元対象/元関係 | 表示対象（前→後） | 要約線（前→後） | 座標上の高さ（前→後） |
|---|---:|---:|---:|---:|
| vehicle-management | 379 / 14,592 | 39 → 39 | 80 → 73 | 3290 → 2548 |
| git-lines | 80 / 18,231 | 9 → 9 | 15 → 12 | 616 → 568 |
| web-atlas | 20 / 8,549 | 9 → 9 | 18 → 18 | 232 → 336 |

vehicle-managementはAPIの起動/既定公開、Web入力と合流するproduction公開、Webの開発配信/ビルド、DB定義からSQL生成/適用を区別。Firebase CLIからAuth Emulator/Suiteへ到達するが、原本入力は未記録なので架空の原本線は足さない。D1の環境別表示集合は各元対象を保持し、ローカル実行をremote資源と同一視しない。

git-linesはWebviewの開発配信とビルドを隣接させる。拡張本体は独立した原本として残し、未確認の起動/公開経路を捏造しない。記録済みビルド出力は本体の経路として残す。Git利用とesbuild成果物、補助構成は同じキャンバスに残る。

web-atlasは短いVite配信経路を手前で終え、共通distからWrangler起動/公開へ分岐。開発配信とビルドを混ぜない。高さの増加は枝見出しの余白であり、原本の縦積み増加ではない。API/DBは足していない。

画像は1600×1100のローカル成果物。修正前vehicle/gitは開始版の既存代表画面、webは旧版を再表示した同条件画面。vehicleの全景Fitと通常倍率は用途を分けて見る。

| 入力 | 修正前 | 修正後 |
|---|---|---|
| vehicle-management | [前](../../.cache/source-branches/vehicle-after.png) | [全景](../../.cache/route-lanes/vehicle-fit.png)・[通常倍率](../../.cache/route-lanes/vehicle-default.png) |
| git-lines | [前](../../.cache/source-branches/git-lines-after.png) | [後](../../.cache/route-lanes/git-lines-after.png) |
| web-atlas | [前](../../.cache/route-lanes/web-before.png) | [後](../../.cache/route-lanes/web-after.png) |

## 正確性・操作・性能

- 3保存入力で全元対象IDの所有先、全元関係IDの内訳/表示線対応を照合。正規モデルのJSON不変を確認。Evidence・環境・条件は正規モデル/線のprovenanceに保持する。
- 詳細な全体・既存プリセットの投影ダイジェスト一致を確認。今回の変更は簡易投影・配置と簡易表示補足に限定。関係ラベルの共通ヘルパーへの追加は簡易専用kindだけ。
- 3入力の2D/3D表示を確認。vehicleでは単クリック前後の位置・両モードのカメラ・scope一致、全体→簡易再訪の保存状態一致。
- webでは原本の明示ボタンから内部へ入り、外側公開先を選んでもscopeが変わらず、プロジェクトへ復帰することを確認。
- 既存のダブルクリック分離、検索、根拠、内訳から全体への移動、ホバー・カメラ・他タブの回帰は通常テストで確認。今回、全操作を3入力それぞれでブラウザ再実施したわけではない。
- 初回生成の単発新旧比較：vehicle 197.1→202.1ms、git-lines 382.9→335.8ms、web 159.3→165.6ms。選択・カメラは再生成入力にしない。
- Chromeのweb保存入力、1600×1100、同じ選択から2回のrequestAnimationFrameまでの参考値は旧90.4ms、現45.1ms。単発・順番実行なので優劣を統計的に断定しない。
- 保存銀行をリセットしていない。セッションはメモリ上で再読込時に再生成され、旧版の永続保存座標移行はない。

## テストと限界

新規 `architectureRouteLanes.test.ts` は用途/任意環境の枝と到着先の同一行、複数原本合流、18独立構成のコンパクト配置、参照束の方向・元ID保存、親を持つ外部起動対象、同等aliasと異なる引数を確認。見出しが前の行へ押し上がらない検査も追加。既存の原本列/囲いテストは新仕様へ更新し、単に削除していない。

全テスト1,417件成功、21件skip（主に環境指定の実入力検証）。追加の3入力比較7件成功。既存全プリセット比較1件成功。見出し再修正後28件、最終投影修正後36件、ページ/型の再確認17件も成功。同入力の重複根拠を同等性に数えない最終調整後も関連20件と実入力比較7件を再確認。lint/typecheck/build/diff-check成功。buildには既存のtree-sitter外部化と大きなchunk警告がある。

大きな図の全景Fitでは文字が小さくなる。通常倍率でパンして読む必要は残る。個々の線の交差・回り込み・粒子量は変更対象外。3Dの投影では重なるラベルがあり、空間操作で読む既存仕様を維持する。全入力の長時間FPS/メモリ測定、全組合せの高速切替、実ブラウザでの全ホバー/検索操作は未網羅。

入力側の動的設定、実際の起動・公開・通信は未実行・未観測。web-atlasの全ソース新規解析は前回Node標準ヒープ上限に達しており、今回も保存モデルによる表示検証。元モデルにない原本入力・到着先を補完しない。
