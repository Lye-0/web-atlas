# タブ3・10の検索行と操作行の整理（2026-09-17）

## 変更

- Command Flowの開始コマンドを通常/全画面、2D/3D共通の主要操作にした。補助操作の折り畳みに入れない。
- 両タブで検索・絞り込み・結果一覧を全画面ルートに含めた。入力を複製せず、全画面切り替え前後で同一DOMと保存状態を使う。
- 表示内容/開始コマンド、2D/3D、全画面操作を常設。狭幅では主要セレクターを独立した行にし、カメラ操作を折り畳む。粒子・囲い・自動省略等は表示設定にまとめた。
- Tab10の環境フィルターは詳細な絞り込みに移し、適用中の環境を閉じた状態でも示す。
- 解析・範囲判定・配置アルゴリズム・カメラ保存・詳細説明は変更していない。

## 自動検証

- pnpm test: 130ファイル成功、9ファイルskip。1,374件成功、14件skip。
- pnpm typecheck / pnpm lint / pnpm build: 成功。buildには既存の大きなchunkの警告あり。
- git diff --check: 成功。
- 開始コマンドの単一表示、全画面内の入力、入力DOMの同一性、状態保持、表示設定のEscape/外側クリック、Tab10の既存設定操作を検証するテストを追加・更新。

## 実ブラウザ

ブラウザ操作スキル（agent-browser 0.38.0、専用Chromeセッション）を使用。vehicle-managementの既存解析済みfixtureを使い、原本は変更・実行していない。

- 1440×1000と390×900で表示を確認。
- Tab3のネイティブ全画面で開始コマンドを2D/3Dそれぞれ変更できた。通常表示へ戻っても検索文字と開始コマンドが保持された。
- Tab10の全画面で検索・追加環境フィルター・表示内容を操作。DB構造変更へ切り替え、全体へ戻ると検索apiとproductionフィルターが復元された。
- Tab10の3D・狭幅で検索結果からAPIを選択し、詳細欄を開いた状態でも検索・表示内容を操作できることを確認。
- Function Call Flowへ移動し、従来の検索・詳細設定・図操作が維持され、新しい表示内容/表示設定が混入しないことを確認。
- 確認中のブラウザ実行エラーなし。

画面記録はローカルの `.cache/control-rows/command-3d.png`、`command-narrow-full.png`、`architecture-fullscreen.png`、`architecture-narrow-detail.png`。

## 制限

- ネイティブ全画面ではChrome自身がEscapeで全画面を終了し、検索文字は保持される。アプリ側の全画面フォールバックでは検索欄のEscape処理を優先する。
- 他タブ全操作・全端末の実ブラウザ総当たりは未実施。共通部品の回帰は全テストとFunction Call Flowの表示確認で補った。
- 狭幅への変更時も既存カメラを保持する。図全体を収め直す場合は「その他の操作」のFitを使う。
