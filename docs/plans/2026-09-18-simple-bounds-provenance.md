# 簡易全体：囲い・根拠・概要の限定修正

開始HEAD: 2fcc8a9、未コミット変更なし。単独実施。

- F1: 支援の囲いはアプリと専用操作を含むものだけ。単独DB/サービス/共有コードや共有支援を囲わない。所属/元ID/線を変更せず、共有2D/3D領域生成に簡易専用の条件を置く。
- F2: simpleMemberCaptionが代表pathと別Evidenceの行を結合している。所属/入口とEvidence位置を分離し、内訳から完全な根拠とプレビュー/コピーへ進める。
- F3: 独立した.NET実行主体は保持。共有コードと既存補助用途は主経路より下の配置へ、DB変更は対象DBの支援として有限の段階順で近接させる。通常線の描画は変えない。
- F4: 外側の元両端をownersへ写し、選択から入る/出るを種類・環境別に説明。内部関係を再出力しない。
- F5: APIの59,205はpath/start/endで重複排除済み。正規化・不明範囲を検証し、入力単位キャッシュで集計説明を追加。元Evidenceは消さない。
- F6: 元コード→操作等の種類を使う一文と、元操作ID数に基づく使用件数を表示。代表scriptは残数を正しく表示する。

既存3スナップショットと同一入力の旧投影を保存し、一般fixture、全体/プリセット不変、ブラウザ操作、全テスト/build/lint/typecheck/diffを確認する。入力原本は読取のみ、公開は行わない。
