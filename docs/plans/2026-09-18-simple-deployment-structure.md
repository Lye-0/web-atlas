# 簡易全体：公開先と構成整理

開始HEAD 33a0cc6、作業ツリー清潔。単独実施。元入力の実行・変更・送信はしない。

- F1: 元flow-deploysの到着先を論理アプリへの吸収から保護。既存実行構成を設定/論理所有者ごとの表示集合として残し、環境別元IDへ戻れるようにする。起動は既存ツール要約を維持。成果物の確定した入力だけを保持する。
- F2: 独立実行主体は保持。共有部分は親/所有者の根拠がある場合だけ内部化。用途未判定のcode-packageは既知の補助と別の集合へ。
- F3: 同じ設定ファイル・binding・資源種類で役割対応を確認できる環境別資源を表示集合へ。実体IDと元線は維持し、同一環境内の曖昧な重複はまとめない。
- F4: 相手の表示IDで1項目にまとめ、方向/種類の短い概要と環境/確度の内訳を分離。
- F5: 支援を道具/成果物/定義/実行構成で分類。公開先は図上に保持。
- F6: 選択とホバー/フォーカスの説明を分離。既存の一時状態の解除を操作とテストで照合し、正常なホバーを残留バグと扱わない。

3保存入力と現行ソースの必要箇所を照合し、同時/別公開、公開なし/未解決、同名別成果物、任意環境、資源の非同一、独立CLI/ライブラリ、双方向相手のfixtureを検証する。全体/既存プリセットの旧実装比較、全テスト/build/lint/typecheck/diffと実ブラウザ操作を行う。
